const teamGrid=document.querySelector('#team-grid');PrincessApi.request('/api/team').then(items=>{teamGrid.replaceChildren();if(!items.length){const empty=document.createElement('div');empty.className='empty-state';empty.textContent='Team profiles are coming soon.';teamGrid.append(empty);return;}items.forEach(item=>{const card=document.createElement('article');card.className='team-card';if(item.hasPhoto){const image=document.createElement('img');image.src=`api/team/${item.id}/photo`;image.alt=item.name;card.append(image);}else{const placeholder=document.createElement('div');placeholder.className='team-photo-placeholder';placeholder.textContent=item.name.charAt(0);card.append(placeholder);}const title=document.createElement('h2');title.textContent=item.name;const role=document.createElement('p');role.className='eyebrow';role.textContent=item.role;const bio=document.createElement('p');bio.textContent=item.bio;card.append(title,role,bio);teamGrid.append(card);});}).catch(()=>{teamGrid.textContent='Team profiles are unavailable right now.';});
